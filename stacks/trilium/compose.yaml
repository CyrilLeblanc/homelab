services:
  app:
    image: triliumnext/notes:stable
    restart: unless-stopped
    networks:
      - web
    # ports:  
    #   - 8080:8080
    volumes:
      - app-data:/home/node/trilium-data:rw

volumes:
  app-data:

networks:
  web:
    external: true